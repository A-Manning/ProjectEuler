/**
 * @author A Manning
 * This problem is essentially fizzbuzz. A (marginally) faster algorithm is possible but this still runs in O(N). 
 */
object Problem1Soln {
   def main(args: Array[String]): Unit = {
      val sum = (1 until 1000).filter(x=> x%3 == 0 || x%5 ==0).sum
      println(sum)
  }
}
